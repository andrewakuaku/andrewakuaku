# macOS
.DS_Store

# Editor
.vscode/
.idea/

# clasp
# Keep both the credentials file (per-user, $HOME) and the project pointer
# (.clasp.json holds the scriptId) out of the public repo. The repo ships
# a .clasp.json.example to copy from.
.clasprc.json
apps-script/.clasp.json
